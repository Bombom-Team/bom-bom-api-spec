# AGENTS.md

## Agent skills

### TypeSpec authoring

When working on TypeSpec or OpenAPI contract changes in this repo, first read `docs/typespec-authoring.md`.
Treat that document as the shared source of truth for authoring workflow, backend mustache customization checks, and when to ask the user for clarification.

### Issue tracker

Issues are tracked in Jira. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles mapped to default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
